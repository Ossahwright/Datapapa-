{
  "buildCommand": "vite build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/paystack-webhook", "destination": "/api/paystack-webhook.ts" },
    { "source": "/api/(.*)", "destination": "/api/$1" }
  ]
}
